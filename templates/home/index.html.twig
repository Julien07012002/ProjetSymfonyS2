{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des produits</h1>

    <ul>
        {% for product in products %}
            <li>
                <h2>{{ product.name }}</h2>
                <p>{{ product.description }}</p>
                <p>Prix : {{ product.price }} €</p>
                <button onclick="window.location.href='{{ path('cart_add', {'id': product.id}) }}'">Ajouter au panier</button>
                <button onclick="window.location.href='{{ path('cart_index') }}'">Voir le panier</button>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
